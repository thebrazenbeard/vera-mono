[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rezon-kernel"
version = "0.0.1"
requires-python = ">=3.12"
description = "Provider-independent epistemic reasoning kernel"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

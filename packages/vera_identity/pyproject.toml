[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vera-identity"
version = "0.1.0"
requires-python = ">=3.12"
description = "Vera identity and bootstrap resources for the self-contained monorepo"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vera-coordination"
version = "0.1.0"
requires-python = ">=3.12"
description = "Vera-owned coordination contracts and runtime"

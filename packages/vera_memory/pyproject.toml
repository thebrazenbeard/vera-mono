[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vera-memory"
version = "0.1.0"
requires-python = ">=3.12"
description = "Monorepo-local governed memory and provenance ledger"

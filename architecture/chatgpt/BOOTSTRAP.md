# Vera Mono Native Project Bootstrap V1

Status: CANDIDATE NATIVE PROJECT INTERFACE

This procedure orients a ChatGPT Project to Vera Mono without treating chat history, embedded control artifacts, or continuation snapshots as canonical runtime state.

1. Read `architecture/chatgpt/CHATGPT_REPO_INTERFACE.yaml`.
2. Fresh-read `thebrazenbeard/vera-mono/main` and record the exact head.
3. Read `README.md` and `architecture/VERA_MONO_MANIFEST_V1.json` on that head.
4. Enumerate relevant open PRs/branches and exact collision subjects.
5. Read the smallest package/contracts needed for the task.
6. For identity/voice work, inspect the current Vera Project Identity and Behavior Profile resources.
7. For execution/effects, inspect `vera_core.QualifiedVeraRuntime`, exact relevant contracts, and task/delegation state.
8. For installation, route, runtime consumption, behavior/effect, attestation, or independent review, obtain evidence from that exact layer.
9. Treat embedded R7A1/R10A0 control source as provenance unless a current exact binding promotes it.
10. Reconstruct active subject ownership; delegated subjects remain mutation-frozen.
11. Bind Patrick's present authority and protected-effect boundary.
12. If Patrick asked to build/run/continue and safe work exists, perform a bounded work unit.
13. Verify effects and persist meaningful engineering state.
14. Report completed work, verification, blockers/conflicts, and exact next frontier.

Currentness rules:

`CHAT != CANONICAL_SOURCE`
`CHECKPOINT != CURRENT_TRUTH`
`REPOSITORY_SOURCE != INSTALLED_RUNTIME`
`PACKAGE_PASS != INSTALL_PASS`
`INSTALL_PASS != CURRENT_ROUTE`
`CURRENT_ROUTE != RUNTIME_CONSUMPTION`
`RUNTIME_CONSUMPTION != BEHAVIOR_PASS`
`BEHAVIOR_PASS != EXTERNAL_EFFECT`
`INTERNAL_ASSURANCE != INDEPENDENT_REVIEW`
`REVIEW_PASS != DEPLOYMENT_AUTHORITY`

If required live evidence is unavailable, keep the claim historical, source-level, inferred, or unknown.

Ordinary conversation does not require a repository scan. Preserve Vera's configured voice while keeping ontology and runtime-currentness claims evidence-bounded.
